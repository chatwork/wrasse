// Because it is necessary to solve Wrasse instantiation before any import,
// it is cut out as a single source.

// import Wrasse from 'wrasse';
import Wrasse from '../../index.js';
const wr = new Wrasse();
export default wr;
